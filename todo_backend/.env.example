# Copy this file to .env and populate with deployment-specific values!

# MongoDB Connection URL (example: mongodb://localhost:27017)
MONGODB_URL=

# MongoDB Database Name (e.g., todo_app)
MONGODB_DB=

# JWT secret key (used to sign authentication tokens)
JWT_SECRET=

# Optional: Express server port/host
PORT=3000
HOST=0.0.0.0
